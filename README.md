# Madinty Ratan — متجر الأثاث الخارجي

موقع متجر إلكتروني بسيط (HTML + CSS + JavaScript عادي) — من غير باك إند ولا خطوة بناء.
الطلبات بتوصل صاحب المتجر على **الواتساب**، وفيه **لوحة تحكم** لمتابعة الطلبات وإدارة المنتجات والأقسام.

---

## التشغيل

افتح `index.html` من المتصفح على طول، أو شغّل سيرفر محلي:

```bash
npx http-server . -p 5599 -c-1
```

---

## الملفات

| الملف | الوظيفة |
| --- | --- |
| `index.html` | الصفحة الرئيسية (هيرو + الأقسام + الأكثر طلبًا + نبذة) |
| `products.html` | جميع المنتجات مع فلتر بالأقسام (`?cat=...`) |
| `product.html` | صفحة المنتج (`?id=MR-XXX-01`) — الأنواع وألوان الشلت والمواصفات |
| `cart.html` | العربة + بيانات العميل + حساب الشحن + الإرسال على الواتساب |
| `about.html` | من نحن |
| `contact.html` | تواصل معنا + خريطة جوجل |
| `admin.html` | **لوحة التحكم** (مش مربوطة في القائمة — احفظ اللينك عندك) |
| `assets/js/data.js` | **كل بيانات المتجر**: المنتجات، الأقسام، الشحن، الألوان، رقم الواتساب |
| `assets/js/app.js` | المنطق المشترك: العربة، الشحن، الطلبات، الهيدر والفوتر |
| `assets/js/admin.js` | منطق لوحة التحكم |
| `assets/css/style.css` | ستايل الموقع |
| `assets/css/admin.css` | ستايل لوحة التحكم |
| `assets/img/products/<القسم>/` | صور المنتجات (webp) |
| `assets/img/categories/` | صور الأقسام (webp) |

---

## التعديلات السريعة (في `assets/js/data.js`)

| عايز تغيّر | غيّر |
| --- | --- |
| رقم استقبال الطلبات | `STORE.whatsapp` (بصيغة دولية بدون +) و `STORE.whatsappDisplay` |
| الإيميل / مكان المصنع / المواعيد | `STORE.email` / `STORE.factory` / `STORE.hours` |
| مكان المصنع على الخريطة | `STORE.map.query` — أي عنوان أو إحداثيات `"30.55,30.98"` |
| مصاريف الشحن | `SHIPPING` — `0` = مجانًا، `null` = تُحدد عند التأكيد |
| ألوان الشلت | `CUSHION_COLORS` |
| الأقسام | `DEFAULT_CATEGORIES` |
| المنتجات | `DEFAULT_PRODUCTS` |

### إضافة منتج يدويًا

```js
{
  id: "MR-IRON-09",                 // كود لا يتكرر
  cat: "iron-sets",                 // لازم يساوي id قسم موجود
  featured: true,                   // اختياري — يظهر في "الأكثر طلبًا"
  name: "طقم حديد علب 6 مقاعد",
  short: "كنبة 3 مقعد + 2 كرسي + ترابيزة",
  img: "assets/img/products/اطقم حديد علب/meatel 9.webp",
  price: 19500, oldPrice: 23000,    // oldPrice اختياري (يظهر كسعر قبل الخصم)
  components: ["كنبة 3 مقعد", "2 كرسي", "ترابيزة"],
  specs: ["حديد علب معالج", "دهان فرن إلكتروستاتك"],
  cushions: true,                   // اختيار لون الشلت إجباري على العميل
  warranty: "ضمان سنتين"            // اختياري
}
```

منتج ليه أكتر من نوع (زي الشمسية والهاموك):

```js
variants: [
  { name: "شمسية 3 متر", price: 4000, oldPrice: 5000 },
  { name: "شمسية 4 متر", price: 5000 }
]
```

لو النوع ليه سعر، السعر ده هو اللي بيتحسب في العربة.

---

## لوحة التحكم — `admin.html`

كلمة السر الافتراضية: **`madinty2025`** — غيّرها من تبويب **الإعدادات** بعد أول دخول.

| التبويب | بيعمل إيه |
| --- | --- |
| نظرة عامة | إجمالي المبيعات وعدد الطلبات والقطع المباعة + آخر الطلبات + الأكثر مبيعًا + مخزون منخفض |
| الطلبات | متابعة الطلبات، تغيير الحالة، التفاصيل، الحذف، تصدير CSV، وإضافة طلب من رسالة واتساب |
| المنتجات | إضافة / تعديل / حذف منتج بكل تفاصيله + المخزون والرواج |
| الأقسام | إضافة / تعديل / حذف قسم |
| الإعدادات | تنزيل `data.js` للنشر، نسخة احتياطية، تغيير كلمة السر، استرجاع الافتراضي |

### المخزون

خانة **المخزون** اختيارية — سِبها فاضية لو مش بتتابع الكميات، وهتظهر «—» في الجدول.
لو حطيت رقم، المنتج بيظهر في لوحة **«مخزون منخفض»** لما يبقى **5 قطع أو أقل**.

### ⚠️ حاجتين مهمين لازم تعرفهم

**1) تعديلات المنتجات والأقسام محفوظة في المتصفح بس.**
الموقع ثابت (static) من غير سيرفر، فاللوحة بتحفظ في `localStorage`. عشان التعديلات تظهر
لكل الزوّار: **الإعدادات ← ⬇ تنزيل data.js** ← استبدل بيه `assets/js/data.js` ← commit & push.

**2) الطلبات مش بتتجمّع أوتوماتيك من كل العملاء.**
لما عميل يعمل أوردر، الطلب بيتسجّل في **متصفح العميل** وبيتبعت لك على **الواتساب**.
عشان تتابع الطلب في اللوحة: افتح **الطلبات ← 📋 لصق رسالة واتساب** والصق الرسالة اللي وصلتك،
واللوحة هتقراها لوحدها وتضيف الطلب بحالته.

> لو عايز الطلبات تتجمّع أوتوماتيك في مكان واحد، ده محتاج باك إند
> (Firebase أو Supabase مثلًا — فيه خطة مجانية تكفي متجر بالحجم ده).

**3) كلمة السر دي حماية شكلية.** الموقع ثابت وأي حد يقدر يقرا ملفاته — متحطش بيانات حساسة في اللوحة.

---

## رحلة الطلب

1. العميل يفتح صفحة المنتج ويختار **لون الشلت (إجباري)** ونوع الطقم لو المنتج ليه أنواع.
2. في `cart.html` يملأ بياناته ويختار المحافظة — **الشحن بيتحسب على طول** من جدول `SHIPPING`.
3. يضغط «تأكيد الطلب» ← بتتفتح نافذة واتساب برسالة جاهزة فيها كل تفاصيل الطلب.
4. الرسالة بتحتوي على اسم المنتج والنوع و**لون الشلت** والكمية والأسعار والشحن وبيانات العميل.
5. **العميل بيضغط زر الإرسال بنفسه** — ده شرط من واتساب، مفيش طريقة تبعت أوتوماتيك من موقع ثابت.

---

## ملاحظات

- كل الصور بصيغة `webp`. صور المنتجات بوسترات طولية (نسبة 3:4 تقريبًا) وبتتعرض كاملة من غير قص.
- أسماء مجلدات وملفات صور المنتجات عربية وفيها مسافات — الكود بيرمّزها أوتوماتيك (`imgURL`).
- التحقق من رقم الموبايل مصري: 11 رقم يبدأ بـ 010 / 011 / 012 / 015.
- مفاتيح `localStorage`: `mr_cart_v1` (العربة) · `mr_db_v1` (تعديلات الكتالوج) · `mr_orders_v1` (الطلبات) · `mr_admin_v1` (كلمة السر).
