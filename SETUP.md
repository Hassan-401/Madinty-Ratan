# ربط الموقع بـ Supabase

الخطوات دي بتتعمل **مرة واحدة بس**. بعدها أي إضافة أو تعديل منتج من لوحة التحكم
بيظهر للزوّار في ثواني، وأي أوردر من الموقع بيوصل اللوحة أوتوماتيك — من غير رفع
ولا إعادة نشر.

الوقت المتوقع: ~10 دقايق.

---

## 1) اعمل مشروع Supabase

1. ادخل [supabase.com](https://supabase.com) واعمل حساب (الخطة المجانية تكفي المتجر ده).
2. **New project** ← اختار اسم وباسورد لقاعدة البيانات (احفظه عندك) ← اختار
   منطقة قريبة (**Frankfurt** أو **London** أقرب حاجة لمصر).
3. استنى دقيقتين لحد ما المشروع يجهز.

---

## 2) جهّز الجداول

1. من القايمة الجانبية: **SQL Editor** ← **New query**.
2. افتح ملف `supabase/schema.sql` من المشروع ده وانسخه كله.
3. **Run**. المفروض يخلص من غير أخطاء.

   الملف فيه إيميلك (`hm2022004@gmail.com`) في جدول `admins` — ده الإيميل
   الوحيد اللي هيقدر يعدّل. لو عايز تضيف حد تاني بعدين، ضيفه من
   **Table Editor ← admins**.

> لو ظهرت أخطاء في آخر جزء (`storage.objects`)، ده معناه إن المشروع مقفّل
> صلاحيات الـStorage من SQL. اعمل الـbucket يدوي: **Storage ← New bucket ←
> الاسم `media` ← فعّل Public**، وبعدين شغّل آخر جزء من الملف تاني.

---

## 3) اعمل حساب الدخول

1. **Authentication ← Users ← Add user ← Create new user**.
2. حط **نفس الإيميل** اللي حطيته في `admins`، وكلمة سر قوية.
3. فعّل **Auto Confirm User** عشان تقدر تدخل على طول.

> 🔒 مهم: من **Authentication ← Sign In / Providers ← Email** اقفل
> **Allow new users to sign up**. مش ضروري (جدول `admins` بيحمي الكتابة برضه)
> بس بيمنع أي حد يعمل حسابات في مشروعك.

---

## 4) وصّل الموقع بالمشروع

1. **Project Settings ← API** وخد القيمتين:
   - **Project URL** — شكله `https://xxxxxxxx.supabase.co`
   - **anon public** key (في المشاريع الجديدة اسمه **publishable key**)
2. افتح `assets/js/config.js` وحطهم:

   ```js
   const SUPABASE_URL = "https://xxxxxxxx.supabase.co";
   const SUPABASE_ANON_KEY = "eyJhbGciOi...";
   ```

3. اعمل commit و push:

   ```bash
   git add assets/js/config.js && git commit -m "ربط Supabase" && git push
   ```

Vercel هيعيد النشر لوحده خلال دقيقة.

> المفتاح ده **عام ومكشوف للكل عن قصد** — ده تصميم Supabase. الحماية الحقيقية
> في قواعد RLS اللي في `schema.sql`: الزائر يقرا المنتجات ويضيف طلب بس، ومش
> بيقدر يقرا طلبات حد تاني ولا يعدّل أي حاجة.
> ⛔ المفتاح الوحيد اللي **ممنوع** يتحط في الكود هو `service_role`.

---

## 5) انقل المنتجات الحالية

1. افتح `https://<موقعك>/admin.html` وسجّل دخول بالإيميل وكلمة السر.
2. **الإعدادات ← 📥 استيراد الكتالوج الافتراضي ← استيراد**.
3. افتح الموقع — المفروض كل حاجة زي ما هي، بس دلوقتي جاية من قاعدة البيانات.

خلاص. جرّب تعدّل سعر منتج من اللوحة وافتح الموقع في تبويب تاني — هتلاقي السعر
اتغيّر على طول.

---

## بعد كده

| عايز تعمل إيه | تعمله منين |
| --- | --- |
| تضيف/تعدّل/تحذف منتج أو قسم | لوحة التحكم — بيظهر للزوّار على طول |
| ترفع صورة منتج جديدة | من نموذج المنتج: **⬆ ارفع صورة من جهازك** (بتروح Supabase Storage) |
| تتابع الطلبات وتغيّر حالتها | تبويب **الطلبات** — الأوردرات بتوصل لوحدها |
| تاخد نسخة احتياطية | **الإعدادات ← ⬇ تصدير نسخة** |

---

## حاجات لازم تعرفها

**الطلبات بتتسجّل قبل ما العميل يبعت الواتساب.** أول ما يضغط «تأكيد الطلب»،
الطلب بيتحفظ في القاعدة وبعدين بتتفتح نافذة الواتساب. يعني ممكن يوصلك طلب في
اللوحة والعميل ما بعتش الرسالة — كلّمه على الرقم اللي في الطلب.

**أي حد يقدر يضيف طلب.** ده لازم عشان الزائر يقدر يطلب من غير تسجيل دخول.
لو لقيت طلبات وهمية، امسحها من اللوحة عادي؛ ولو المشكلة كبرت، ابعتلي نضيف
تحقّق (Turnstile أو تأكيد الرقم).

**لو Supabase وقع أو النت فصل**، الموقع بيرجع يعرض آخر نسخة اتحمّلت من الكتالوج
(أو النسخة اللي في `assets/js/data.js`) بدل ما يظهر فاضي. لوحة التحكم بس هي
اللي محتاجة اتصال فعلي.

**`assets/js/data.js` بقى نسخة احتياطية.** الكتالوج الحقيقي في قاعدة البيانات
دلوقتي. سيبه زي ما هو — بيشتغل كشبكة أمان ومصدر لزرار الاستيراد.

---

## لو حاجة مشيت غلط

| المشكلة | السبب غالبًا |
| --- | --- |
| «محتاج إعداد» في صفحة اللوحة | `config.js` لسه فاضي أو الرفع ما تمّش |
| «الإيميل أو كلمة السر غلط» | راجع المستخدم في Authentication، واتأكد إنه Confirmed |
| «الحساب ده مش مسجّل كأدمن» | إيميلك مش في جدول `admins` — ضيفه من **Table Editor** |
| «فشلت العملية: new row violates row-level security» | نفس السبب اللي فوق |
| الموقع بيعرض منتجات قديمة | إعمل Hard refresh (Ctrl+Shift+R) — النسخة المتخزنة في المتصفح |
| «تعذّر الاتصال بـ Supabase» | الـURL غلط في `config.js`، أو المشروع متوقف (Supabase بيوقف المشاريع المجانية بعد أسبوع سكون — افتح الداشبورد عشان يرجع) |

---

## لما تشتري دومين

الموقع مربوط دلوقتي بـ `https://madinty-ratan.vercel.app`، والعنوان ده مكتوب
في كذا مكان (روابط canonical، وبيانات المشاركة على فيسبوك، وsitemap، وrobots).

**في Vercel:** Project ← Settings ← Domains ← Add، وبعدين اربط الدومين من عند
الشركة اللي اشتريت منها (Vercel بيوريك الـ DNS المطلوب). خلّي الدومين الجديد هو
الـ **Primary** عشان الروابط القديمة تتحوّل عليه.

**في الكود:** لازم تتغيّر `madinty-ratan.vercel.app` للدومين الجديد في:

| الملف | فيه إيه |
| --- | --- |
| `index.html` وباقي الصفحات | `canonical` + `og:url` + `og:image` |
| `robots.txt` | سطر `Sitemap:` |
| `sitemap.xml` | كل الروابط |

قول لي الدومين وأنا أغيّرها كلها وأرفعها.

> **مهم:** من غير التغيير ده، لما تشارك لينك الموقع على فيسبوك أو واتساب
> هيفضل يعرض العنوان القديم.

---

## حاجات مقصودة (مش مشاكل)

**مشاركة منتج معيّن على فيسبوك بتعرض صورة الموقع العامة مش صورة المنتج.**
عشان تعرض صورة المنتج نفسه، الصفحة لازم تتبني على السيرفر، والموقع دلوقتي
ثابت بالكامل (وده اللي بيخليه سريع ومجاني). لو ده مهم، ينفع نعمله بعدين
بـ Vercel Function.

**لو بدّلت صورة في `assets/img` بنفس اسمها، زوّد رقم الإصدار.**
الصور متخزنة في متصفح الزائر شهر كامل عشان السرعة، فلو حطيت صورة جديدة بنفس
الاسم هيفضل شايف القديمة. الحل: افتح `assets/js/app.js` وزوّد `ASSET_V`
واحد (مثلًا من `"2"` لـ `"3"`) وارفع — كل الصور هتتحدّث عند الكل على طول.

> الصور اللي بترفعها من **لوحة التحكم** مش محتاجة الخطوة دي — Supabase بيدّي
> كل صورة اسم جديد أوتوماتيك.

> فيه كمان أربع صور مكتوبة في الكود مباشرة (`hero` و`home` و`about`) وليها
> `?v=2` مكتوب جنبها في `style.css` و`index.html` و`about.html` — لو غيّرت
> واحدة فيهم، غيّر الرقم ده كمان.
